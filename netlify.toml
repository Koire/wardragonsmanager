[build]
    command = "npm run build"
    publish = "/dist"
    base = "/"
[[redirects]]
    from = "/*"
    to = "/index.html"
    status = 200
[functions]
    node_bundler = "esbuild"
    directory = "functions"
[dev]
    command="npm run develop"
    port=9095